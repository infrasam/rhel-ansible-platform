---
# Master playbook — Configures a server from zero to hero.
# Runs with: ansible-playbook site.yml
# Run it partially:    ansible-playbook site.yml --tags baseline
# Run it against a specific host:    ansible-playbook site.yml --limit rhel-node1

- import_playbook: playbooks/baseline.yml
- import_playbook: playbooks/users_ssh.yml
- import_playbook: playbooks/selinux.yml
- import_playbook: playbooks/sysctl_hardening.yml
- import_playbook: playbooks/firewall.yml
- import_playbook: playbooks/packages.yml
- import_playbook: playbooks/logging.yml
